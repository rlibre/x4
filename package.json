{
  "name": "webcomp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs --watch --hmr --serve",
    "build-release": "node scripts/build.mjs --release --serve"
  },
  "devDependencies": {
    "chalk": "^5.3.0",
    "esbuild": "^0.24.0",
    "esbuild-sass-plugin": "^3.3.1",
    "watcher": "^2.3.1",
    "ws": "^8.18.0"
  },
  "x4build": {
    "entryPoints": [
      "src/main.tsx"
    ],
    "copy": [
      {
        "from": "src/assets",
        "to": "assets"
      }
    ]
  },
  "dependencies": {
    "@fontsource/montserrat": "^5.1.0"
  }
}
